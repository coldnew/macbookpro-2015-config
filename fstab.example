# /etc/fstab: static file system information.
#
# noatime turns off atimes for increased performance (atimes normally aren't 
# needed); notail increases performance of ReiserFS (at the expense of storage 
# efficiency).  It's safe to drop the noatime options if you want and to 
# switch between notail / tail freely.
#
# The root filesystem should have a pass number of either 0 or 1.
# All other filesystems should have a pass number of 0 or greater than 1.
#
# See the manpage fstab(5) for more information.
#

# <fs>			<mountpoint>	<type>		<opts>		<dump/pass>

# NOTE: If your BOOT partition is ReiserFS, add the notail option to opts.
#/dev/BOOT		/boot		ext2		noauto,noatime	1 2
#/dev/ROOT		/		ext3		noatime		0 1
#/dev/SWAP		none		swap		sw		0 0
#/dev/cdrom		/mnt/cdrom	auto		noauto,ro	0 0
#/dev/fd0		/mnt/floppy	auto		noauto		0 0

# NOTE: If your BOOT partition is ReiserFS, add the notail option to opts.
/dev/sda4   /                  ext4    defaults,noatime,discard,data=writeback        0 1
/dev/sda5   /boot/efi          auto    defaults                                       0 0
/dev/cdrom  /mnt/cdrom         auto    noauto,ro                                      0 0

tmpfs       /dev/shm           tmpfs   defaults                                       0 0
#tmpfs       /tmp               tmpfs   nodev,nosuid,size=2G                           0 0
devpts      /dev/pts           devpts  gid=5,mode=620                                 0 0
sysfs       /sys               sysfs   defaults                                       0 0
proc        /proc              proc    defaults                                       0 0
tmpfs       /var/log           tmpfs   defaults,noatime,mode=1777,size=20m            0 0
tmpfs       /var/tmp           tmpfs   defaults,noatime,mode=1777,size=5m             0 0
tmpfs	    /var/tmp/portage   tmpfs   uid=portage,gid=portage,mode=0775,size=4096M,noatime	0 0
